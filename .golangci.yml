version: "2"
linters:
  settings:
    errcheck:
      # CLI output to stdout/stderr; write errors are not actionable here.
      exclude-functions:
        - fmt.Print
        - fmt.Printf
        - fmt.Println
        - fmt.Fprintf
        - fmt.Fprint
        - fmt.Fprintln
